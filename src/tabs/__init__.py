"""Tab package for the F1 Race Intelligence dashboard.

Each tab lives in its own module under src/tabs/ and exposes a single
render function (render_strategy, render_driver_battle, ...). Shared
constants and helpers live in _shared so the dashboard shell stays thin
and tab logic is isolated for safe editing.
"""
